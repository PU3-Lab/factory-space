# superpowers-harness Request

## User Request

Superpowers 방식으로 backend agent harness를 구성한다.

## Acceptance Checks

- repo-local harness team spec이 존재한다.
- coordinator, implementer, spec reviewer, quality reviewer skill이 존재한다.
- backend agent 개발 규칙이 현재 `backend/src` 구조와 연결된다.
- 결정 사항이 `backend/src/DECISION_LOG.md`에 기록된다.
- 구조 검증과 backend 테스트/lint를 fresh run으로 확인한다.
