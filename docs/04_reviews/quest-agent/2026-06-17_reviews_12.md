# 코드 리뷰 — reviews_11 검증 리뷰 (Phase 3 & 4 정합성 확인)

- **날짜**: 2026-06-17
- **리뷰 번호**: 12
- **대상**: `reviews_11.md`(Phase 3 & 4 구현 검증 문서) 및 대응 코드/커밋
- **대상 커밋**: `ec9e59c`(Phase 3, QuestRuleGenerator), `b529b92`(Phase 4, QuestValidator)
- **리뷰어**: Claude (코드 리뷰)
- **결과**: ⚠️ **Approve with comments** — 코드/검증 내용은 사실이나, 문서의 커밋 스코프 표기 및 미해결 항목 단정에 정정 필요.

---

## 1. 검증 요약

`reviews_11`이 기술한 주장을 실제 코드·커밋·테스트 실행 결과와 1:1 대조했다.

검증 환경: `backend/.venv`

| 항목 | 결과 |
|---|---|
| `pytest tests/test_quest_rule_generator.py` | ✅ 5 passed |
| `pytest tests/test_quest_validator.py` | ✅ 10 passed |
| `ruff format --check` (validator + rule_generator) | ✅ 2 files already formatted |
| `pytest -q` (전체 회귀) | ✅ 358 passed |

---

## 2. 사실 확인 결과

### ✅ 정확한 주장

| reviews_11 주장 | 검증 근거 |
|---|---|
| `generate_drafts`가 우선순위 정렬된 리스트 반환 | `rule_generator.py:20` `def generate_drafts`, `:37` `return []` |
| 메인 목표 조기 차단 버그 → `continue` 우회로 해소 | rule_generator 탐색 로직 확인 |
| `_can_produce` 재귀 DFS로 다단계 체인 검증 | `validator.py:100-151` |
| `visited` 집합으로 순환참조 무한루프 차단 | `test_validate_fail_circular_dependency` 통과 |
| `import unittest.mock` 파일 상단 이동 | `test_quest_validator.py:3` |
| 15개 단위 테스트 통과 (rule 5 + validator 10) | ✅ 5 + 10 |
| `pytest -q` → 358 passed | ✅ 재현 |

### 🟠 Major — 커밋 스코프 표기 부정확

- 위치: `reviews_11.md` 5번째 줄(대상 커밋), 14~18번째 줄(대상 파일)
- 내용: 대상 커밋을 `b529b92` 단일로 명시하면서 `rule_generator.py`·`test_quest_rule_generator.py`(Phase 3)를 대상 파일에 포함시켰다. 실제 커밋 구성은 다음과 같다.
  - `b529b92`(Phase 4) = `validator.py` + `test_quest_validator.py` + 메모 2건 **만**
  - Phase 3 파일(`rule_generator.py`, `test_quest_rule_generator.py`)은 **별도 커밋 `ec9e59c`** 소속
- 영향: 변경 추적성 저하. 추후 커밋 단위로 회귀/롤백 시 혼동 유발.
- 제안: 제목이 "Phase 3 & 4 통합 검증"이므로 대상 커밋에 `ec9e59c`와 `b529b92`를 **둘 다 명시**.

### 🟡 Minor

**m-1. 이전 리뷰(reviews_11 변경 전)의 Minor 5건 미반영 상태가 "0건"으로 단정됨**
- 내용: `validator.py` 대상으로 제기됐던 Minor 5건이 코드에 아직 그대로 존재한다.
  - 매직 넘버 `1000`/`0` (`validator.py:62`)
  - Pydantic과 중복되는 도달불가 방어분기 `invalid_type`/`amount<=0` (`validator.py:54,62`)
  - `target_id` 미정규화 (`validator.py:46`)
  - 원재료 도달가능성 무조건 `True` 가정 (`validator.py:131-133`)
  - 테스트 데이터 의존 불일치 (실 CSV vs mock)
- 영향: `reviews_11` 3절은 "현재 미해결 Blocker/Major/Minor 없음"으로 단정하나, 위 항목은 미해결 상태다.
- 제안: 해당 5건을 *수용/후속 Phase 이연*으로 명시하거나 별도 트래킹 항목으로 남겨 상태를 정직하게 반영.

**m-2. 줄 수 제한 표기 불일치**
- 위치: `reviews_11.md` 4절 "줄 수 제한(500줄 이하)"
- 내용: 프로젝트/글로벌 코딩 규칙 상한은 **800줄**이다(`validator.py` 151줄, `rule_generator.py` 98줄로 둘 다 충분히 하회 — 사실 판정엔 영향 없음).
- 제안: 표기 상한을 800으로 정정하거나 근거를 명시.

---

## 3. 종합 상태

- **코드 동작·테스트·포맷 검증 내용은 전건 사실**이며 품질 양호.
- 문서 차원에서 **커밋 스코프(Major)** 1건과 **미해결 Minor 단정(Minor)**·**줄 수 표기(Minor)** 2건의 정정이 필요.
- 미해결 Minor 5건은 기능 결함이 아닌 품질/유지보수 권고로, 후속 Phase 이연 가능.

---

## 4. 긍정적인 부분

- Phase 3/4 핵심 알고리즘(우선순위 초안 리스트, 재귀 선행조건 탐색, 순환참조 차단)이 엣지 케이스 테스트로 단단히 검증됨.
- 358개 전체 회귀 무결성 유지 — 신규 컴포넌트 추가가 기존 코드에 회귀를 일으키지 않음.
- mock 기반 다단계/순환 테스트로 마스터 데이터 비의존 검증 라인을 확보.
