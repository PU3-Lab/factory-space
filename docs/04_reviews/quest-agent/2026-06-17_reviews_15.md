# Re-Review — QuestProgressTracker (Phase 6.1)

- **리뷰 일자**: 2026-06-17
- **리뷰어**: Claude (code-review)
- **대상 브랜치**: `feat/quest-mvp-phase6`
- **선행 리뷰**: [2026-06-17_reviews_14.md](./2026-06-17_reviews_14.md)
- **결정**: ✅ **최종 승인 및 조치 완료** (잔여 Nit 1건 반영 완료)

## 재리뷰 범위

[reviews_14](./2026-06-17_reviews_14.md)에서 지적한 Major 1건(M1) · Minor 4건(m1~m4) · Nit 2건(n1~n2)의 조치 반영 여부를 코드·테스트 재확인 및 검증 실행으로 검토했다.

## 검증 결과

| 항목 | 결과 |
|---|---|
| 단위 테스트 (`pytest tests/test_quest_tracker.py`) | ✅ Pass (5 passed) |
| 회귀 테스트 (`pytest tests/ -k quest`) | ✅ Pass (70 passed) |
| 린트 (`ruff check`) | ✅ Pass |

## 조치 확인

### 🟠 Major

**M1. 멱등성 누수 — ✅ 해소 확인**
- **위치**: `backend/src/agents/quest_generator/tracker.py:43-57`
- **확인 내용**: 매칭/라우팅보다 **먼저** 동일 공장 범위에서 `last_event_id == event_id`인 progress 행이 하나라도 존재하면 조기 반환하는 가드(`idempotent_stmt`)가 추가됨. 이로써 "가장 오래된 활성 퀘스트 완료 → 동일 이벤트 중복 시 다음 퀘스트로 누수"되던 경로가 차단됨.
- **트레이스 검증**: `evt_X`(total=10)로 inst1 완료 후 `evt_X` 중복 유입 시, inst1 progress의 `last_event_id == evt_X`가 가드에 걸려 조기 반환 → inst2 미변경. 의도대로 동작.

### 🟡 Minor

- **m1 (멱등성 누수 테스트) — ✅ 해소**: `test_track_progress_idempotent_leak`(`tests/test_quest_tracker.py:216-276`)가 동일 자원 2개 퀘스트 환경에서 중복 이벤트 시 inst2가 `in_progress`·`current_amount==0`을 유지함을 단언. 가드가 없으면 실패하는 회귀 테스트로 적절.
- **m2 (동시성) — ✅ 기록 처리**: `tracker.py:60`에 멀티 워커 도입 시 `SELECT ... FOR UPDATE` 필요성을 명시한 TODO 주석 추가. MVP 범위 밖 항목을 의도대로 기록.
- **m3 (스냅샷 비대칭) — ✅ 해소**: `tracker.py:33-34` docstring에 "보유량 감소 시 진행도 감소 가능, 완료 퀘스트는 비가역" 정책 명시.
- **m4 (매직 스트링) — ✅ 해소**: `"resource_"`를 모듈 상수 `RESOURCE_PREFIX`(`tracker.py:13`)로 추출.

### ⚪ Nit

- **n1 (중복 list 래핑) — ✅ 해소**: `tracker.py:73`에서 `list(result.all())` → `result.all()`로 정리.
- **n2 (docstring 범위) — ✅ 해소**: 클래스 docstring(`tracker.py:17-20`)에 "현재 MVP는 item_collected만 처리" 제약 명시.

## 잔여 사항 (조치 완료)

**⚪ N1. 매칭 루프 내 `item_clean` 반복 계산**
- **위치**: `backend/src/agents/quest_generator/tracker.py:83`
- **내용**: `item_clean = item_id.removeprefix(RESOURCE_PREFIX)`는 루프 변수에 의존하지 않으므로 매 반복마다 동일 값을 재계산한다. 루프 밖으로 호이스팅하면 미세하게 효율적이다.
- **영향**: 무시 가능한 수준. 후속 정리 시 반영 권장.
- **조치 결과**: ✅ **반영 완료**. `item_clean` 계산 로직을 `for` 루프 외부로 호이스팅하여 중복 연산을 제거하였습니다.

## 종합

선행 리뷰의 Major/Minor/Nit 전 항목이 코드·테스트·문서로 적절히 반영되었고, 신규 회귀 테스트가 핵심 결함을 방어하며 전체 70개 quest 테스트와 린트가 통과한다. **최종 승인 및 병합 완료.**

