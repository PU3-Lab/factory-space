# 코드 리뷰 — Quest Router B1 회귀 수정 재검토 (Task 7.1)

- **날짜**: 2026-06-17
- **리뷰어**: Claude (code-review)
- **대상 브랜치/상태**: 작업 트리 미커밋 변경 (reviews_17 B1 조치분)
- **결정**: ✅ **APPROVE** — Blocker/Major 없음, 머지 가능

## 변경 요약

reviews_17에서 지적한 🔴 Blocker(B1: m3 정규화가 중복 차단 로직을 깨뜨린 회귀)가 권고안(제안 1)대로 해결되었다.

| 항목 | 결과 |
|---|---|
| 테스트 (`pytest tests/test_quest_router.py`) | ✅ 4 passed |
| 린트 (`ruff check`) | ✅ All checks passed |
| 회귀 검증 (동일 아이템 재요청) | ✅ 400 차단 (테스트로 고정) |

---

## B1 조치 검증

| 검증 항목 | 결과 |
|---|---|
| `active_target_ids`가 원문 `target_id`를 그대로 담도록 롤백 (`quest_router.py:50-54`) | ✅ 확인 |
| 라우터에서 `removeprefix`/`RESOURCE_PREFIX` 데드코드 제거 | ✅ 확인 (잔존 없음) |
| `rule_generator.py:43` / `validator.py:70` 비교와 정합성 복구 | ✅ 원문 기준 일치 |
| 회귀 테스트 추가 (`test_quest_router_duplicate_target_blocked`) | ✅ 상한 미만 + 동일 아이템 → 400 검증 |

- **위치**: `quest_router.py:50-54`, `tests/test_quest_router.py:242-279`
- **내용**: 라우터가 활성 퀘스트의 `target_id`를 정규화 없이 집합에 담아, 이를 소비하는 `rule_generator`/`validator`의 원문 비교와 다시 일치한다. 동일 아이템 재요청 시 후보 초안이 비어 `400`(`"...all already active"`)을 반환한다. 신규 E2E가 이 경로를 직접 검증한다.
- **재검증 (실측)**: reviews_17에서 2건 생성되던 시나리오가 이제 2번째 요청에서 `400`으로 차단됨을 테스트 통과로 확인.

---

## 잔여 이슈

없음. reviews_16의 6건(M1·M2·m1·m2·m3·n1·n2) 및 reviews_17의 B1까지 모두 해소되었다.

> 후속(비차단) 참고: M2의 인가(`# TODO(auth)`)는 MVP 의도로 보류된 상태이며, 멀티테넌트 정식화 시 별도 태스크로 처리 권장. n1 관련 테스트가 커밋 동작 자체를 검증하지 않는 점도 동일하게 향후 통합 테스트에서 보강 여지가 있으나, 현 변경 범위에서는 차단 사유 아님.

## 긍정적인 부분

- 회귀를 KISS 원칙대로(공유 모듈 미변경, 라우터 단일 지점 롤백) 최소 변경으로 해결했다.
- 회귀 테스트가 "상한 미만 + 동일 아이템"이라는 정확한 빈틈 시나리오를 겨냥해, 동일 회귀의 재발을 영구 방어한다.
- 데드코드(`RESOURCE_PREFIX`)까지 함께 정리해 변경을 깔끔하게 마무리했다.
