# 리뷰: Quest Agent MVP 작업 계획 · reviews_05 조치 검증 (U-1·U-2 수렴 확인)

- 일자: 2026-06-17
- 대상:
  - [docs/02_work_plans/2026-06-16-quest-agent-mvp-plan.md](../../02_work_plans/2026-06-16-quest-agent-mvp-plan.md)
  - [docs/04_reviews/2026-06-16_reviews_05.md](2026-06-16_reviews_05.md)
- 선행 리뷰: [reviews_05](2026-06-16_reviews_05.md)
- 성격: 코드 변경 없음. reviews_05의 Nit 이슈 U-1·U-2 조치가 계획서에 실제 반영됐는지 검증.

## 변경 요약

reviews_05에서 Nit으로 분류된 2건(U-1: `shortage_amount` dead field, U-2: title/description 템플릿 미정의)에 대해 조치가 이루어졌다. 계획서 본문과 대조한 결과, **2건 모두 정확히 반영되었으며 신규 Blocker·Major는 없다.**

### 검증 결과 요약

| 이슈 | 조치 위치 | 판정 |
|---|---|:---:|
| U-1 (`shortage_amount` dead field 해소) | `quest-mvp-plan.md:30` "description 생성에 소비"로 수정, `:133` Phase 3 소비처 명시 | ✅ |
| U-2 (title/description 템플릿 정의) | `quest-mvp-plan.md:134-137` — `title`, `description` f-string 템플릿 명시 | ✅ |

## 이슈 목록

### 🔴 Blocker

없음.

### 🟠 Major

없음.

### 🟡 Minor

없음.

### ⚪ Nit

**[V-1] Phase 3에서 `item_name` 조회 출처 미명시**

- 위치: `2026-06-16-quest-agent-mvp-plan.md:135-136`
- 내용: 새로 정의된 `title`/`description` 템플릿이 `{item_name}`을 참조하지만, Phase 3 본문에서 이 값을 어디서 가져오는지(예: `game_data.py`의 `resources.csv` 로더 via `item_id`) 명시하지 않았다. Phase 2에서 `game_data.py`를 구성하는 내용이 있으나, Phase 3에서의 조회 연결이 암묵적이다.
- 영향: 구현자가 `item_name` 조회 경로를 추론해야 하므로, 오해나 중복 구현 소지가 있다.
- 제안: Phase 3에 `item_name = game_data.get_item_name(item_id)` 또는 `resources.csv` 로더 참조를 한 줄 추가.
- **조치 결과 (Resolved)**: Phase 2에 `resources.csv`로부터 자원명 매핑(`item_id -> 자원명`)을 로드하도록 계획을 구체화하였고, Phase 3에서 `game_data` 로더를 통해 `item_id`에 해당하는 한국어 자원명(`item_name`)을 조회하도록 명시했습니다. (`2026-06-16-quest-agent-mvp-plan.md:123`, `:135`)

**[V-2] reviews_05의 "조치 결과" 라인 참조 소폭 부정확**

- 위치: `2026-06-16_reviews_05.md:34`, `:41`
- 내용: reviews_05 U-1 조치 결과의 라인 참조가 `:133-134`인데, 실제 변경 후 `:134`는 `SupportQuestDraft 생성 및 반환:` 헤더 라인이 됐다. 템플릿 세부 내용은 `:135-137`에 위치한다. U-2의 조치 결과도 `:134`를 참조하나 동일한 이유로 미세하게 오프셋됐다.
- 영향: 극미. 라인 번호가 1~2줄 벗어났을 뿐이며 내용은 정확하다.
- 제안: 필요 시 `:133-137`로 범위를 넓혀 재명시. 선택 사항.
- **조치 결과 (Resolved)**: `reviews_05.md` 내 U-1 및 U-2의 조치 결과 라인 참조 범위를 실제 템플릿 세부 내용 위치에 맞게 `:133-137` 및 `:134-137`로 수정하여 오프셋을 보정했습니다. (`2026-06-16_reviews_05.md:34`, `:41`)

## 우선순위 권고

1. U-1·U-2 포함 전체 추적 이슈가 수렴했다. **계획서는 구현 착수 가능한 상태를 유지.**
2. [V-1] — ✅ Resolved (계획서 보완 완료)
3. [V-2] — ✅ Resolved (라인 참조 오프셋 보정 완료)

## 긍정적인 부분

- U-1과 U-2를 단일 편집으로 함께 해소한 설계가 간결하다: `shortage_amount`를 `description` 템플릿의 소비처로 삼아 두 이슈를 동시에 닫았다.
- `description` 템플릿(`{shortage_amount}개가 더 필요합니다. 총 {target_amount}개를 모으세요.`)이 사용자에게 맥락과 목표를 모두 전달하는 실용적인 문구다.
- reviews_05에 "조치 결과 (Resolved)" 주석을 인라인으로 추가한 방식이 이전 라운드들과 일관되어 추적성이 유지된다.
