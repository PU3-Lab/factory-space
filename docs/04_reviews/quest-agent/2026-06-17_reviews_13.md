# 코드 리뷰 — reviews_11 정정 재검증 (Phase 3 & 4)

- **날짜**: 2026-06-17
- **리뷰 번호**: 13
- **대상**: 갱신된 `reviews_11.md`(reviews_12 권고 반영본) 및 대응 커밋
- **대상 커밋**: `ec9e59c`(Phase 3, QuestRuleGenerator), `dd8791e`(Phase 4, QuestValidator — 현재 HEAD)
- **리뷰어**: Claude (코드 리뷰)
- **결과**: ⚠️ **Approve with comments** — 정정 2건 반영 양호. Phase 4 커밋 해시 1건 미정확 잔존, reviews_12의 줄 수 지적은 철회.

---

## 1. 검증 요약

`reviews_12`가 제기한 정정 권고가 `reviews_11`에 반영됐는지, 그리고 그 과정에서 새 부정확이 유입되지 않았는지 재대조했다.

검증 환경: `backend/.venv`, `git log`

---

## 2. 정정 반영 결과

| reviews_12 지적 | 심각도 | 상태 | 검증 내용 |
|---|---|---|---|
| 미해결 Minor 5건이 "0건"으로 단정됨 | 🟡 Minor | ✅ 반영 | `reviews_11.md:48-54`에 "미해결 이연 항목(Minor 5건)"으로 명시 분류, 후속 Phase 이연(기술 부채)으로 정직하게 표기. |
| 커밋 스코프 — Phase 3 누락 | 🟠 Major | ✅ 부분 반영 | Phase 3 커밋 `ec9e59c`를 추가 표기(해시 정확). 단, Phase 4 해시는 아래 잔존 이슈 참조. |

---

## 3. 잔존/정정 이슈

### 🟠 Major — Phase 4 커밋 해시 부정확

- 위치: `reviews_11.md:5`, `:12`
- 내용: Phase 4 커밋을 `10c6eb7`로 표기했으나, 현재 브랜치 HEAD의 Phase 4 커밋은 **`dd8791e`**다. `10c6eb7`은 `git log` 브랜치 이력에 없는 **버려진(orphaned) amend 커밋**이다.
  - 세션 시작 시점 `b529b92` → `10c6eb7` → `dd8791e`로 두 차례 amend되며 해시가 연쇄 변경됨.
- 근본 원인: `reviews_11.md`가 **자기 자신을 포함하는 커밋**(Phase 4 커밋이 reviews_11.md 본문을 포함)을 참조한다. 문서를 고쳐 amend할 때마다 해시가 바뀌어 **자기참조가 구조적으로 불안정**하다.
- 영향: 문서가 가리키는 해시가 실제 이력과 불일치 → 추적성 저하.
- 제안 (택1):
  1. 해시를 `dd8791e`로 갱신 — 단, 이 문서를 포함해 또 amend하면 재발.
  2. **(권장)** 커밋 해시 대신 커밋 메시지/Phase 명으로만 참조하고 해시 표기는 생략 — 자기참조 불안정성을 원천 회피.

### ✅ 철회 — reviews_12 m-2(줄 수 상한 800) 오류 정정

- 내용: `reviews_12`는 "코딩 규칙 상한은 800줄"이라 지적했으나 이는 **글로벌 규칙(common/coding-style.md)** 기준이었고, 본 프로젝트는 `AGENTS.md`로 500줄을 강제한다.
  - `AGENTS.md:26`: "일반 source 파일은 500줄을 넘기지 마세요"
  - `backend/AGENTS.md:193`: 동일
- 판정: `reviews_11.md:64`의 "AGENTS.md 기준 500줄 이하" 표기는 **정확**하다. reviews_12 m-2 지적은 **무효 처리**한다.

---

## 4. 종합 상태

- 코드 동작·테스트(rule 5 / validator 10 / 전체 358)·포맷 검증은 reviews_11/12에서 이미 사실로 확인됨 — 본 회차에서 회귀 없음.
- 문서 정합성: Minor 5건 분류·Phase 3 해시·줄 수 근거는 정확. **Phase 4 커밋 해시(`10c6eb7` → 실제 `dd8791e`)** 1건만 미정확으로 잔존.
- 권고: 자기참조 해시 불안정성을 고려해 Phase 4는 해시 대신 메시지로 참조.

---

## 5. 긍정적인 부분

- reviews_12 권고를 단순 수용이 아니라, 근거(AGENTS.md 500줄)를 들어 일부 지적(800줄)을 역으로 바로잡은 점은 건강한 리뷰 캐치볼이다.
- 미해결 Minor를 숨기지 않고 "이연 항목"으로 명시해 기술 부채 가시성을 확보했다.
