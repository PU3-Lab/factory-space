# 코드 리뷰 — reviews_09 잔여 항목(m-b/m-c/m-d) 조치 검증

- **날짜**: 2026-06-17
- **리뷰 번호**: 10
- **대상 커밋**: `74d4f90` (reviews_09 피드백 반영 및 잔여 항목 선제 조치)
- **리뷰어**: Claude (코드 리뷰)

---

## 1. 변경 요약

reviews_08/09에서 후속 Phase 권고로 분류했던 Minor 3건(m-b/m-c/m-d)을 선제 조치한 커밋(`74d4f90`)을 검증했다.

대상 파일: `pyproject.toml`, `context_builder.py`, `game_data.py`, `tests/test_quest_context_builder.py`.

검증 환경: `backend/.venv`
- `ruff check ...` → **All checks passed!**
- `pytest tests/test_quest_context_builder.py` → **5 passed**
- `pytest -m "not integration"` → **4 passed, 1 deselected** (마커 정상 동작 확인)

---

## 2. 조치 검증 결과

| 항목 | 심각도 | 상태 | 검증 내용 |
|------|--------|------|-----------|
| m-b 테스트 강결합 | 🟡 Minor | ✅ 해소 | `pyproject.toml`에 `[tool.pytest.ini_options]` `integration` 마커 등록, `test_game_data_loader`에 `@pytest.mark.integration` 부여. `-m "not integration"`로 해당 테스트가 정상 deselect됨을 확인. |
| m-c 다중 산출물 손실 | 🟡 Minor | ✅ 해소 | `game_data.py:78` 단일 산출(`output_ids[0]`) 한계와 향후 다중 산출 확장 시 고려 사항을 NOTE 주석으로 명시. (동작 변경 없음, 의도적 한계 문서화) |
| m-d payload 사전 검증 | 🟡 Minor | ✅ 해소 | `context_builder.py`에서 `required`/`current`를 raw dict 값으로 직접 산술하던 흐름을 제거. 먼저 `MainQuestObjective`(Pydantic, `required>0`/`current>=0`)를 생성한 뒤 `objective_model.required`/`.current`로 `shortage_amount`를 계산하도록 변경 → 경계 검증 후 연산. |

→ 잔여 Minor 3건 모두 코드에 정상 반영. 회귀 테스트 통과, 린트 0건.

### 품질 메모
- m-d 처리: 검증된 모델 필드로 연산 입력을 일원화해, 비정상 타입/음수 유입 시 `KnownIssue` 생성 전에 `ValidationError`로 빠르게 실패한다. "경계에서 검증" 원칙에 부합.
- m-b 처리: 마커만 부여하고 기본 `pytest` 실행에는 여전히 포함되므로 CI에서 누락되지 않으며, 필요 시 `-m "not integration"`으로 데이터 비의존 빠른 실행이 가능해졌다.

---

## 3. 종합 상태

지원 퀘스트 MVP Phase 1/2 코드(데이터 모델 + game_data 로더 + QuestContextBuilder)에 대해 reviews_07~09에서 제기된 모든 이슈가 해소되었다.

| 리뷰 | 제기 | 해소 |
|------|------|------|
| reviews_07 | M1, M2, m1, m2, m3, n1 (6) | ✅ 전건 |
| reviews_08 | B1, M-a, m-a, n-a (4) | ✅ 전건 |
| reviews_09 잔여 | m-b, m-c, m-d (3) | ✅ 전건 |

**현재 미해결 Blocker/Major/Minor 없음.** 검증 기준(ruff clean, 테스트 green) 충족.

---

## 4. 긍정적인 부분

- 후속 Phase로 미뤄도 됐을 Minor 3건까지 선제적으로 마무리해 기술 부채를 남기지 않았다.
- m-d를 우회(예: 방어적 if 분기)가 아닌 도메인 모델 검증 경유로 처리해 단일 검증 출처를 유지했다.
- 테스트 마커 도입으로 단위/통합 실행을 분리할 수 있는 기반을 마련했다.
- 의도적 한계(m-c)는 숨기지 않고 NOTE로 명시해 다음 작업자에게 컨텍스트를 남겼다.
