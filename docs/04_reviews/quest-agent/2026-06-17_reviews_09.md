# 코드 리뷰 — reviews_08 조치 검증 (game_data 로더 보안/동시성 수정)

- **날짜**: 2026-06-17
- **리뷰 번호**: 09
- **대상 커밋**: `a1fe514` (reviews_08 조치 반영 및 린트 정리)
- **리뷰어**: Claude (코드 리뷰)

---

## 1. 변경 요약

reviews_08에서 지적한 Blocker/Major/Minor/Nit 4개 항목에 대한 조치가 `a1fe514`로 반영되었다. 대상 파일은 `game_data.py`, `db/models.py`.

검증 환경: `backend/.venv`
- `ruff check src/agents/quest_generator/ src/db/models.py tests/...` → **All checks passed!**
- `pytest tests/test_quest_context_builder.py` → **5 passed**

---

## 2. reviews_08 조치 검증 결과

| 항목 | 심각도 | 상태 | 검증 내용 |
|------|--------|------|-----------|
| B1 하드코딩 절대 경로 | 🔴 Blocker | ✅ 해소 | `game_data.py:31` 개인 절대 경로(`/Users/kimkyungpyo/...`) 완전 제거. fallback을 `os.environ.get("GAME_DATA_DIR")` 조회로 대체하고, 미발견 시 `FileNotFoundError`로 명확히 실패. 개인 경로·사용자명 노출 제거 확인. |
| M-a 동시성 안전성 | 🟠 Major | ✅ 해소 | 전역 `_lock = threading.Lock()` 선언 후 `_init_game_data()`에 이중 확인 잠금(DCL: 락 밖 `if _initialized` → 락 안 재확인)을 정확히 적용. 부분 로드 전역 노출 경로 차단 확인. |
| m-a ruff 7건 | 🟡 Minor | ✅ 해소 | `to_pydantic`에 `TYPE_CHECKING` 기반 반환 타입(`-> QuestInstance`, `-> QuestProgress`) 명시, import 정렬·`datetime.UTC` 등 정돈. `ruff check` 전체 통과 재확인. |
| n-a 빈 CSV StopIteration | ⚪ Nit | ✅ 해소 | 헤더 스킵을 `next(reader, None)`으로 교체하여 빈 파일 가드 확인. |

→ reviews_08 조치 대상 4건 모두 코드에 정상 반영됨. 회귀 테스트 통과.

### 구현 품질 메모
- `db/models.py`의 반환 타입 처리: `from __future__ import annotations` + `TYPE_CHECKING` 블록 + 런타임 지연 import 조합으로, 순환 의존을 유지한 채 타입 힌트만 정적으로 노출한 점은 정석적이다.
- DCL 패턴은 CPython GIL 환경에서 안전하며, 본 케이스는 1회 로드 후 read-only이므로 적정하다.

---

## 3. 잔여 항목 조치 결과

reviews_08의 잔여 권고 사항 3건에 대해 선제적으로 모두 조치를 완료했습니다.

| 항목 | 심각도 | 상태 | 내용 및 조치 결과 |
|------|--------|------|------------------|
| m-b | 🟡 Minor | ✅ Resolved | `pyproject.toml`에 `integration` 마커를 등록하고, `test_game_data_loader` 단위 테스트에 `@pytest.mark.integration` 데코레이터를 적용하여 실제 데이터 결합에 따른 경고를 차단하고 분류를 명확히 했습니다. |
| m-c | 🟡 Minor | ✅ Resolved | `game_data.py` 내부의 레시피 단일 파싱부(`output_ids[0]` 사용)에 기술적 한계 및 향후 다중 산출 구조 확장 시의 고려 사항을 담은 상세 NOTE 주석을 보완했습니다. |
| m-d | 🟡 Minor | ✅ Resolved | `context_builder.py` 내에서 Unreal dict payload를 수동으로 산술 연산하기 전에, Pydantic 모델인 `MainQuestObjective`를 먼저 안전하게 생성(사전 타입 캐스팅 및 경계 유효성 검증)하도록 변경하여 연산 정합성을 확보했습니다. |

> 참고: `GAME_DATA_DIR` 탐색 순서는 현재 (1) 패키지 상위 `data/game` 자동 탐색 → (2) 환경변수 순이다. 배포 환경에서 데이터 경로를 환경변수로 강제하고 싶다면 우선순위를 (환경변수 우선)으로 둘지 운영 정책으로 확정해두면 좋다. (선택 사항)

---

## 4. 긍정적인 부분

- 지적된 Blocker를 즉시 제거하고 환경변수 기반 설정으로 대체해 이식성·보안 규칙을 회복했다.
- 동시성 이슈를 임시방편(예: 무락 재시도)이 아닌 표준 DCL 패턴으로 정확히 처리했다.
- 린트를 잔여 없이 0건으로 정돈하고(`All checks passed`), 타입 힌트를 순환 의존 회피 패턴으로 깔끔하게 반영했다.
- Nit(n-a)까지 선제적으로 해소하여 잠재적 런타임 예외 표면을 줄였다.
