# 2026-06-19 코드 리뷰 06 — 튜토리얼 이벤트 문자열을 #306 슬롯 경로에 맞춤

- **대상**: PR #306(빌드 숫자키 카테고리 슬롯 연결) 후 채굴기/송전탑 튜토리얼 이벤트 문자열 불일치 수정
- **리뷰 범위**: `QuestManagerSubsystem.cpp` 2줄(listener 상수 값). ⚠️ **Chan 영역 파일이나 Chan 허락 하에** JJ가 그 2줄만 수정.
- **리뷰어**: code-reviewer 에이전트
- **PIE**: 통과(TUT_MINING_001 / TUT_POWER_003 진행 확인)

## 배경

PR #306이 숫자키를 LDJ `UI_BuildModeMain::ExecutePlacementMode`로 라우팅 → 이벤트가 `FString::Printf("Select%sMode", *SubsystemKey)`(UI_BuildModeMain.cpp:268)로 발화. 채굴기 SubsystemKey=`"MinerMachine"` → `"SelectMinerMachineMode"`, 송전탑 SubsystemKey=`"PowerGridNode"` → `"SelectPowerGridNodeMode"`. 그런데 퀘스트 listener(`QuestManagerSubsystem.cpp` TUT_MINING_001/TUT_POWER_003)가 옛 문자열을 청취해 #306 후 두 튜토리얼 스텝이 조용히 깨짐.

## 변경

`QuestManagerSubsystem.cpp` 상수 **값만** 2개(이름·구조 불변):
- `TutorialEventSelectMinerMode` `"SelectMinerMode"` → `"SelectMinerMachineMode"`
- `TutorialEventSelectPowerNodeMode` `"SelectPowerNodeMode"` → `"SelectPowerGridNodeMode"`

## 검증 (전부 PASS)

1. **새 문자열이 발화 문자열과 EXACT 일치**: `Printf("Select%sMode", SubsystemKey)` + registry SubsystemKey("MinerMachine"/"PowerGridNode") 교차 확인. ✅
2. **상수 이름 불변** → `:251`/`:271` `SetEventRequirement` 호출 그대로 컴파일, 새 문자열 요구. ✅
3. **다른 사용처 없음** — 각 상수는 정의(`:35`/`:38`) + 단일 `SetEventRequirement`만. 부수 파급 0. ✅
4. **SubsystemKey 무변경** — "MinerMachine"/"PowerGridNode"(아이콘 조회 + PlaceMachine 요구 TUT_MINING_003/TUT_POWER_004)는 안 건드림. `git diff --stat` 1파일 2줄 확인. ✅

## 이슈 (Blocker 0 · Major 0 · Open Question 1)

### ⚪ Open Question (LOW, 비차단) — 옛 문자열 caller 잔존
- `OJJ_Player.cpp:954/1024` `SetPowerNodeMode`/`SetMinerMode`가 옛 `"SelectPowerNodeMode"`/`"SelectMinerMode"` 발화. 이 핸들러는 IA_*에 바인딩돼 코드상 살아있으나, **#306이 IMC 숫자키 매핑을 제거해 런타임 미발화**(어느 키도 안 가리킴). 이 fix 후 옛 문자열은 listener가 없어 orphaned이나, 미발화라 무해.
- **조치**: Group A 죽은코드 청소 패스서 핸들러 제거 시 동반 정리. 이번 비차단.

## 우선순위 권고
- **승인(APPROVE)**. 최소·외과적·자기완결. #306 후속 버그fix.

## 긍정적인 부분
- 상수 값만 바꾸고 이름 보존 → 호출부 보존 + 단일 진실원 유지.
- 발화 문자열(Printf + registry key 계약)에 동적 정합 — 분기 divergence 차단.
- Chan 영역 최소 침범(2줄), 협의·허락 하 수정.
