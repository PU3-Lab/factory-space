# 2026-06-20 코드 리뷰 08 — Foundation 물 판정을 "안 가려진 WaterArea"로 5중 통일 + GroundZ 베이크 활성

- **대상**: WaterArea 물 판정을 전역 깊이(-20/-980 baked WaterCells) 휴리스틱 → **"안 가려진 WaterArea"(WaterArea 덮임 AND 지형 GroundZ < 면 Z = 드러난 웅덩이)** 로 재정의. 색·배치·시각Z·철거·머신배치 5경로 통일. GroundZ 베이크 활성(L_Planet 캐시).
- **리뷰 범위**: `OJJ_Grid.h/.cpp`, `OJJ_BuildController.cpp`, `L_Planet.umap`(GroundZ 캐시)
- **리뷰 방식**: codex 교차검증(각 버그 진단) + codex 최종 정합 review(7경로) + PIE 반복
- **PIE**: 통과 — 묻힌 땅(WaterArea 덮임·uncoveredWater=0)에 Foundation 색/배치/철거/머신 OK, 드러난 웅덩이만 물, 깊은 구덩이(-20 오판정) 해소
- **빌드**: 통과 (#318 머지 후 새 베이스 재빌드)

## 핵심 헬퍼
`OJJ_IsUncoveredWaterCell(Cell)`: GroundZ 무효 시 baked WaterCells(-20) 폴백 → `GetWaterSurfaceZAtCell`(WaterArea 면 Z) 덮임 AND `GetActorLocation().Z + GetCellGroundZ(delta) < 면 Z` → 실제 물.

## 5중 통일 (codex 최종 review: OVERALL COHERENT)
1. **색** `OJJ_ClassifyCellColor` — 3모드 물 판정 전부 `OJJ_IsUncoveredWaterCell`.
2. **배치** `CanPlaceFoundation` — 물 거부 `OJJ_IsUncoveredWaterCell` + occupancy `OJJ_IsLiquidResourceOccupant` 면제.
3. **시각Z** `OJJ_GetCellVisualBaseZInternal` — 드러난 물만 수면Z, 묻힌 셀 지형Z fall-through(버그픽스: 옛 "덮이면 무조건 수면" → 묻힌 타일 은닉).
4. **철거** `DemolishUnderCursor`/`UpdateDemolishHover` — 액체 WaterArea nullify → Foundation 폴백, `OJJ_CountOccupiedFoundationCells` WaterArea 면제.
5. **머신배치** `CanPlaceMachine` 게이트 B — `OJJ_IsLiquidResourceOccupant` 면제(묻힌 땅 Foundation 데크 위 머신 허용). 게이트 A가 bare WaterArea(Foundation 없음) 일반 머신 차단 유지.

## 검증 (codex 7경로 PASS)
- 전역 `IsCellWater`(=baked WaterCells -20)는 펌프 bWaterCellOk·파이프·컨베이어용으로 **미변경(의도적 공존)** — 블래스트 회피. ✅
- bare WaterArea에 일반 머신은 게이트 A(IsCellConstructible false)가 거부 → occupancy 면제로 새로 허용 안 됨. ✅
- 진단코드(DumpWaterDiag/OJJ.Grid.WaterDiag) 완전 제거, dangling 없음, 빌드 통과. ✅

## GroundZ 베이크 활성 (Phase B)
- `bBakeGroundHeights=true` + RebakeAndCache로 L_Planet 베이크 캐시에 per-cell GroundZ(int16) 영속 → off-grid 경고 해소, "안 가려진 WaterArea" 비교(지형 vs 면 Z) 전제 충족.

## 이슈 (Blocker 0 · Major 0 · Minor 0 · Nit 1)
### ⚪ Nit — 두 water 정의 공존
- Foundation 경로=새 정의(안 가려진 WaterArea), 전역 IsCellWater=옛 -20(펌프/파이프). 의도적 공존(주석 명시). 향후 전역 통일은 별도 검토.

## 우선순위 권고
- **승인(APPROVE)**. 5경로 단일 기준 통일(codex COHERENT), 회귀 격리(전역 미변경), PIE·빌드 통과.

## ⚠️ 팀 영향 — 맵(.umap) 커밋
- **L_Planet GroundZ 베이크 활성** → umap 캐시 변경. 바이너리 맵 last-write-wins — Chan/SSR이 같은 L_Planet 수정 중이면 머지 순서 주의. (현 #318까지 main은 L_Planet 미변경 확인, clean FF.)

## 긍정적인 부분
- 5경로를 단일 헬퍼/면제 패턴으로 통일 — 색=배치=시각Z=철거=머신 일관.
- codex 교차검증으로 각 숨은 경로(시각Z 은닉, 철거 그림자, 머신 게이트 B) 발굴.
- 전역 water -20 미변경으로 펌프/파이프 무회귀.
