# 2026-06-19 코드 리뷰 04 — 공용 빌드키 재배치(F/G/H/Z) + 사다리 C→H + IA 경로 폐기

- **대상**: 공용(카테고리 무관) 빌드키 재배치 + 옛 Foundation/Ramp Enhanced-Input 경로 폐기 + 죽은 코드 정리
- **리뷰 범위**: `OJJ_Player.cpp`/`.h`(BindKey·핸들러·죽은코드), `OJJ_BuildController.cpp`/`.h`(enum None·None 가드). `.uasset`(IMC_Build 매핑정리, IA 2개 삭제, BP_OJJ_Player 참조정리)은 에디터 산출물 — 리뷰 범위 외.
- **리뷰어**: code-reviewer 에이전트 (Codex companion Bash 권한 차단 → Claude 레인 대체)
- **PIE**: 통과(F/G/H/Z 작동, Z 취소 OK, G 이중발화 없음, IMC 정리됨)

## 변경 요약

빌드모드 공용키(카테고리 무관, 항상 동작) 재배치:
| 키 | 동작 |
|---|---|
| F | 평면 Foundation (`OJJ_SelectFoundationKind(false)`) |
| G | 경사 RampFoundation (`OJJ_SelectFoundationKind(true)`) |
| H | 사다리 (기존 C BindKey에서 이동) |
| Z | 마우스 초기화 = `SetPlacementMode(None)` (고스트 취소, 빌드모드 유지) |
| X | 철거 (기존 유지) |

- 전부 **레거시 BindKey + 핸들러 `IsInBuildMode()` 가드**(Demolish/Ladder Shortcut 패턴).
- `EOJJ_BuildPlacementMode::None` **맨끝 append**(BP 직렬화 안전). `UpdateMouseHover`/`OnLeftClickPressed`에 None 가드(고스트 클리어·무동작).
- 옛 G=Foundation/H=Ramp Enhanced-Input 경로 폐기: IMC_Build 매핑 제거 + IA_SetFoundationMode/IA_SetRampMode **.uasset 삭제**(에디터) → C++의 죽은 UPROPERTY·BindAction·핸들러 제거. 살아있는 경로 `OJJ_SelectFoundationKind` 유지.

## 검증 (리뷰어 6개 포커스 — 전부 PASS)

1. **enum None 맨끝 append** — 기존 값 재정렬/중간삽입 없음, BP 안전. ✅
2. **None 모드 완전성** — `EOJJ_BuildPlacementMode::` 100+ 사용처 grep: UpdateMouseHover(early-out)·OnLeftClickPressed(early-out)·OnLeftClickReleased(드래그 커밋만, None fall-through 무해)·footprint/class 가드·UI 핫바 switch(default-safe) 전부 None을 inert 처리. SetPlacementMode(None)이 드래그 취소 후 전환 → 반쪽 고스트 없음. Z=고스트 클리어·무배치·빌드모드 유지·무크래시 **검증**. ✅
3. **죽은 코드 제거** — `SetFoundationMode`/`SetRampFoundationMode`/`IA_SetFoundationMode`/`IA_SetRampMode` 잔여 참조 **0**(dangling BindAction·UFUNCTION·exec·UPROPERTY 없음). `OJJ_SelectFoundationKind` F/G로 도달. ✅
4. **신규 Shortcut 핸들러** — 3종 모두 `IsInBuildMode` + BuildController null 가드(기존 패턴 일치). ✅
5. **C→H 이동 + 키 충돌** — EKeys::C 사다리 잔여 없음. 레거시 BindKey 집합 M/Slash/J/Tab/I/Enter/O/P/T/X + 신규 F/G/H/Z 중복·이동 충돌 없음. ✅
6. **dangling** — 스테일 주석 정리(아래 반영). ✅

## 이슈 목록 (Blocker 0 · Major 0 · Minor 2 · Nit 1)

### 🟡 Minor — F가 IA_Interact와 공유(설계상, 역가드 의존) — **주석 반영**
- **위치**: `OJJ_Player.cpp` F BindKey vs IA_Interact(OnInteract)
- **내용**: F가 빌드모드 중=Foundation / 밖=Interact. 상호배타는 `SetFoundationModeShortcut`(IsInBuildMode 요구)와 `OnInteract`(IsInBuildMode면 early-return)의 **역가드**에 의존. 현재 정상(리뷰어 HIGH 확신). IA_Interact가 IMC_Build로 옮겨가거나 가드가 빠지면 이중발화 위험.
- **조치**: F BindKey에 "IA_Interact와 공유, 역가드 상호배타" 주석 1줄 추가.

### 🟡 Minor — `RotateHoverClockwise` 필터에 Pump/Pipe 부재 (선재, 범위 외)
- None 작업과 무관. None은 필터 통과(early return)라 영향 없음. 조치 없음.

### ⚪ Nit — 스테일 주석(옛 키 표기) — **수정 완료**
- `OJJ_BuildController.cpp:417` "G=평판/H=램프" → "F=평판/G=램프"로 수정(현 매핑과 오인 유발 해소).

## 우선순위 권고
- Blocker/Major 없음 → **승인(APPROVE)**. Minor 1(F 공유 주석)·Nit(417 스테일) **반영 완료**. Minor 2는 선재·범위 외.
- ⚠️ 후속: **카테고리 숫자키 연동은 LDJ `UI_BuildModeMain`(EBuildSubMode/레지스트리/ExecutePlacementMode)과 협의 필요** — 이번은 공용키만.

## 긍정적인 부분
- append-only enum 규율 + BP 직렬화 경고 주석.
- None early-out을 hover/click 디스패치 최상단에 배치(부분 상태 위험 0).
- 죽은 코드 제거가 대칭적·철저(BindAction+UPROPERTY+선언+정의 동시).
- 신규 핸들러가 기존 Shortcut 가드 패턴 충실 미러.
- SetPlacementMode가 전환 전 드래그 취소 → Z→None이 반쪽 컨베이어/전선 드래그 안 남김.
